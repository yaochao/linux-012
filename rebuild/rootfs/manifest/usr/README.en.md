# `rebuild/rootfs/manifest/usr/`

[中文 README](./README.md)

This directory stores files or subdirectories copied into the guest `/usr` subtree. At the moment it mainly contains the `/usr/root` contents.

## Subdirectory

- `root/`
  contents of the guest working directory after login
