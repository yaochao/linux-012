# `rebuild/rootfs/manifest/usr/root/`

[English README](./README.en.md)

这个目录保存 guest `/usr/root` 下的初始文件。这里的文件会出现在 Linux 0.12 启动后的默认工作目录中。

## 当前文件

- `README`
  guest 内可见的提示文件，用来引导测试者先执行 `ls`、`pwd` 等命令
