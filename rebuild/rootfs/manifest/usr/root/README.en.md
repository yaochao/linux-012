# `rebuild/rootfs/manifest/usr/root/`

[中文 README](./README.md)

This directory stores the initial files placed under guest `/usr/root`. These files appear in the default working directory after Linux 0.12 boots.

## Current File

- `README`
  visible hint file inside the guest, used to guide the tester toward `ls`, `pwd`, and related commands
