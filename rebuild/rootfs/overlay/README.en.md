# `rebuild/rootfs/overlay/`

[中文 README](./README.md)

This directory is reserved for future overlay files that may be copied into the root filesystem. The repository does not currently place any real content here.

## Current State

- the directory is empty
- only `.gitkeep` is present so git tracks the directory
